distance = float(input("Enter the distance travelled in kilometres: "))

time = float(input("\t\n Enter the time taken in hours: "))

speed = distance / time

print(f"\t\n The speed is: {speed} kilometers per hour")